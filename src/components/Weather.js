import React from 'react'
import BottomNavBar from './BottomNavBar'

function Weather() {
    return (
        <div>
            weather
            <BottomNavBar />
        </div>
    )
}

export default Weather
